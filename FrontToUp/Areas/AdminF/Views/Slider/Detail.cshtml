@model SliderImage
@{
    ViewData["Title"] = "Detail";
}

    <div class="row">
        <img src="~/img/@Model.ImageName" width="400px" height="200px" />
    </div>
    <div class="row mt-3">@Model.ImageName</div>
    <div class="row mt-3">
        <a asp-action="Index" class="btn btn-dark">Go back</a>
    </div>

